# Engagement scope & authorization

> **The agents read this file first.** If the scope is empty or unclear, the
> orchestrator will ask you to fill it in before doing anything intrusive.

## Authorization

- [ ] I am authorized to test the targets below (engagement / bug bounty / lab I own).
- Authorization reference (contract / program / ticket): `__________`

## In-scope targets

```
# One target per line: hostnames, IPs, CIDRs, URLs, API base paths.
# Example:
# 10.10.10.0/24
# https://app.example.com
```

## Out-of-scope (do NOT touch)

```
# Anything that must never be tested. The HITL policy will still gate
# intrusive actions, but list known exclusions here.
```

## Rules of engagement

- Allowed activities: (recon / web / exploitation / ...)
- Time window:
- Points of contact:
- Constraints (no DoS, no data destruction, rate limits, etc.):
