# Dependencies
node_modules/
runtime/plugin/node_modules/
runtime/node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment & secrets — NEVER commit these
.env
.env.local
*.pem
*.key
auth.json

# opencode local state (must never leak into the image or repo)
.opencode/auth.json
opencode.db*

# Logs & runtime artifacts
logs/
*.log

# A real engagement workspace created next to the launcher (never commit loot)
/engagement/

# Per-agent model selection (user-specific; generated by `lynx models`)
lynx.models.json

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
